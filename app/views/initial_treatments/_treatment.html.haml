= div_for treatment do
  %h3
    %span.actions
      = link_to 'Delete', patient_treatment_path(:patient_id => @patient, :id => treatment), :confirm => 'Are you sure?', :method => :delete
  = treatment.treatment
